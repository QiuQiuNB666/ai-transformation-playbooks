# 诚一设计规范

用于客户提案、决策看板、对客物料、独立站。所有配色已通过对比度与色盲可辨性验证，验证方法与数值见文末。

适用范围：网页、PDF、演示文稿、打印物料。不含 logo 与品牌形象系统，那需要先定诚一的核心符号（见最后一节）。

---

## 一、配色

### 1.1 完整 token（直接复制进 CSS）

```css
:root{
  /* 底与面 */
  --bg:#F8F6FC;        /* 页面底 */
  --surf:#FFFFFF;      /* 卡片与图表面 */
  --panel:#EFEBF9;     /* 次级面：表头、引用块 */
  --line:#DCD6EE;      /* 分隔线与边框 */

  /* 文字 */
  --ink:#1E1B4B;       /* 正文与标题 */
  --ink2:#44406C;      /* 次级文字、表格内容 */
  --muted:#6E6A92;     /* 弱化：说明、单位、脚注 */

  /* 数据序列（深到浅，有序分级用） */
  --s1:#4C1D95;
  --s2:#7C3AED;
  --s3:#A78BFA;

  /* 强调与语义 */
  --acc:#5B21B6;       /* 链接、强调、左边框 */
  --neg:#B4483C;       /* 负向：下降、劝退、风险 */
  --grid:#E7E2F4;      /* 图表网格 */

  /* 警示块 */
  --warn-bg:#FDF1DA;
  --warn-fg:#7A4405;
  --warn-line:#EAC377;
}
```

深色模式只改 token，不改组件样式。两个作用域都要写：媒体查询管系统设置，`data-theme` 管手动切换。

```css
@media (prefers-color-scheme:dark){:root:not([data-theme="light"]){
  --bg:#141127; --surf:#1C1837; --panel:#231E44; --line:#38325F;
  --ink:#EDEAF9; --ink2:#CBC6E6; --muted:#9C97C2;
  --s1:#C4B5FD; --s2:#8B5CF6; --s3:#5B21B6;
  --acc:#A78BFA; --neg:#E08A7D; --grid:#2B2652;
  --warn-bg:#3A2A0F; --warn-fg:#F3CE83; --warn-line:#6A4914;
}}
:root[data-theme="dark"]{ /* 同上，完整重复一遍 */ }
```

注意深色模式下序列色的顺序反了：`--s1` 变成最浅。这是对的，因为深底上浅色才是"强"。

### 1.2 什么时候用哪个色

| 用途 | 色 | 规则 |
|---|---|---|
| 有序分级（该做 / 值得试 / 劝退） | s1 → s2 → s3 | 一个色相由深到浅，每段必须直接标数字 |
| 两类对比（我方 vs 同行、固定 vs 可变） | acc 与 neg | 这一对色盲可辨性已验证，可放心并列 |
| 负向变化（下降、恶化、风险、禁止） | neg | 只用于真的变坏，不当第四个系列色用 |
| 单系列条形与折线 | acc 或 s1 | 单系列不要图例，标题里写清是什么 |
| 时间压力、截止日、红线 | warn 三色 | 整块用，不零散用 |

### 1.3 三条硬规则

- **不用双轴。** 两个量级差很多的指标就分两张图，或者换算成同单位（例如"获客成本相当于几年客单价"）。
- **色不跟排名走，跟实体走。** 筛掉几个系列之后，剩下的颜色不能重排。
- **超过两个系列时图例必须在，四个以内还要直接标数字。** 不能只靠颜色区分身份。

---

## 二、字体

### 2.1 屏幕

```css
--serif:"Noto Serif SC","Songti SC","STSong","SimSun",serif;   /* 标题 */
--sans:"Noto Sans SC","PingFang SC","Hiragino Sans GB",sans-serif; /* 正文 */
```

标题衬线、正文无衬线，这个对比是层次的主要来源。Google Fonts 只从 `fonts.googleapis.com` 引，其他字体源在 Artifact 环境里会静默失败。

### 2.2 打印与 PDF

**苹方是 Apple 系统字体，许可上不允许嵌入 PDF；Google Fonts 的中文分片在离线渲染时加载不全。** 所以打印时必须换本地字体栈：

```css
@media print{
  :root{
    --sans:"Songti SC","STSong","PingFang SC",serif;
    --serif:"Songti SC","STSong","SimSun",serif;
  }
  h1,h2,th,td:first-child,.pill,svg .lab{font-weight:700}
}
```

标题和正文都成了宋体，所以层次改用**字号加字重**，不再靠字族。中文印刷品本来就是宋体正文，可读性没问题。

### 2.3 字号

| 场景 | 屏幕 | 打印 |
|---|---|---|
| 正文 | 15px | 10pt |
| H1 | 22 到 29px | 19pt |
| H2 | 17.5px | 13pt |
| 图表标题 | 14px | 起自正文 |
| 说明与脚注 | 12px | 8.5pt |
| 表格 | 13px | 8.5pt |
| SVG 内文字 | 12px | 8.5pt |

正文行宽控制在 68 个字符内，行高 1.65。

---

## 三、组件

固定这几个，不要每次发明新的。

**卡片**：`--surf` 底、1px `--line` 边、6px 圆角、13 到 15px 内距。不要给每个块都加阴影，阴影只留给真正要浮起来的那一个。

**表格**：表头 `--panel` 底加粗；首列用 `--ink` 加粗；数字列加 `font-variant-numeric:tabular-nums` 和 `white-space:nowrap`；负向数字用 `--neg`。外层套 `overflow-x:auto` 的容器，页面本身永远不横向滚动。

**pill 标签**：3px 圆角、11.5px、加粗。深紫底白字、中紫底白字、浅紫底深字、砖红底白字、面色底加边框表示"已删除"。

**点阵强度**：三格方块表示强弱，比文字"强/中/弱"直观。用于护城河、优先级这类分级。

**引用块**：左边 4px `--acc` 竖条加 `--panel` 底，右侧圆角。用于一句话结论。

**警示块**：warn 三色，整块用。用于截止日、硬止损、法律红线。

**时间轴**：一条横线加节点，当前位置用加粗线段表示已过去。红线位置单独用 `--neg` 竖线标出。

---

## 四、图表规则

1. **先定形式再定颜色。** 先问这组数据的任务是什么：比大小用条形，比构成用堆叠，看趋势用折线，单个数字就别画图直接做大字。
2. **区间用区间条，不要用两个端点。** 价格带、工时范围这类，一根从下限到上限的条比两个点清楚得多。
3. **数量级悬殊不要强行同图。** 要么换算成可比单位，要么分图。悬殊本身是结论时（例如一单身份等于多少户代账），就用同一把尺子让它自己说话。
4. **每段都标数字。** 浅色段上用深字，深色段上用白字。
5. **图表必须能退回表格。** 复杂图配一个可展开的 `<details>` 表格视图，这同时满足无障碍要求。
6. **每个图形元素都要有 `<title>`**，鼠标悬停能看到完整数值与口径。
7. **SVG 的 viewBox 要给最外侧标签留空间**，否则标签会被裁掉。底部说明文字与轴标签之间至少留 20 个单位。

---

## 五、数据口径纪律

这一条不是审美，是信誉。

| 标注 | 含义 | 能不能进 KPI |
|---|---|---|
| 官方原文 | 政府网站、法规全文、官方收费表 | 能 |
| 第三方统计 | 有方法学披露的独立调查 | 能 |
| 行业媒体转述 | 媒体报道但未回溯一手来源 | 不能，只作方向 |
| 服务商自述 | 厂商、平台、代理自己的材料 | 不能 |
| 从业者自述 | 问卷、访谈、自报数据 | 不能 |
| 本文估算 | 我方推算，未经实测 | 不能 |
| 待核实 | 查不到出处 | 不能对外引用 |

**每个数字都要带标注。没有标注的数字视为写错了，应当删掉。** 拿不到价格就写"需询价"，不要编区间。

---

## 六、要避开的做法

这几样会让物料看起来像随手生成的：

- 紫蓝渐变的大英雄区
- emoji 当章节标记
- 所有内容居中
- 每个块都圆角加阴影
- 一句话里塞多个数字
- 大量小字假正文填版面
- 彩虹配色，或者在分歧色的中点放一个色相

---

## 七、可复用模板

`模板.html` 是一个空骨架，包含完整 token、双主题、打印样式、以及卡片、表格、pill、点阵、区间条、堆叠条、折线的示例代码。新做页面直接复制它改内容。

生成 PDF：

```bash
"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" --headless --disable-gpu \
  --virtual-time-budget=15000 --print-to-pdf-no-header \
  --print-to-pdf=输出.pdf "file:///绝对路径/页面.html?print=1"
```

URL 末尾的 `?print=1` 会让页面把所有 `<details>` 展开，否则 PDF 里表格视图是折叠的。

---

## 八、验证方法

配色不靠眼睛判断，用脚本跑。

```bash
cd <dataviz skill 目录>/scripts
node validate_palette.js "#5B21B6,#B4483C" --mode light
```

检查五项：亮度带、彩度下限、色盲可辨性、正常视力可辨性、与底色的对比度。

**已验证结果：**

| 组合 | 结果 |
|---|---|
| 两类对比 acc 与 neg（浅色） | 五项全过，色盲可辨性 ΔE 23.8，正常视力 ΔE 29.0 |
| 两类对比（深色） | 三项全过，ΔE 19.3 与 20.1 |
| 序列紫三档 | 作为序列色使用，检查亮度单调性而非类别可辨性，配直接数字标签 |
| 全部文字与底色组合 | 深浅两个模式共 17 组，全部达到正文级 4.5:1 |

序列色跑类别验证器会按设计失败，那不是真失败，不要为了让它通过而改一个本来合理的色阶。

---

## 九、还没做的：品牌形象系统

这份规范管的是**配色与排版**，不含 logo、图形语言、品牌隐喻。那一层需要先回答几个只有你能定的问题：

- 诚一的核心隐喻是什么。深港之间的桥、通关口、印章与凭证、还是别的。
- 调性偏哪边。机构与可信（偏正式，适合合规业务），还是敏捷与现代（偏科技，适合智企业务）。
- 三个板块共用一个标识，还是主副标识分开。

定了之后可以用 `brandkit` skill 生成完整的品牌概览板，包含 logo 概念、构造逻辑、应用场景、配色系统、字体样张。那是图像生成，产出的是提案用的视觉板，不是可编辑的矢量文件。
